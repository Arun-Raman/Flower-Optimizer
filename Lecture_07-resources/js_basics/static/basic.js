function myFunction() {
    var person = prompt("Please enter your name:");
    confirm("Are you sure you want to input: " + person);
    alert ("This is what you entered: " + person);
  
  }

// myFunction();